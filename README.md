# Shell Scripting Course

## Vagrant commands

```
vagrant box add debian/buster64 #Download a debian 10 image


vagrant init debian/buster64 #Bring and run a debian 10 image
vagrant up #Create a VM

vagrant destroy #Destroy a VM

vagrant ssh #connect via ssh a VM
vagrant share #Deploy a VM to the world"
```
